import { ItemView, WorkspaceLeaf, setIcon } from 'obsidian';
import type PracticePlugin from '../main';
import { Question } from '../types/question';
import { ConfirmDeleteModal } from './ConfirmDeleteModal';
import { QuestionEditModal } from './QuestionEditModal';
export const QUESTION_BANK_VIEW_TYPE='practice-question-bank';
export class QuestionBankView extends ItemView{constructor(leaf:WorkspaceLeaf,private readonly plugin:PracticePlugin){super(leaf);}getViewType():string{return QUESTION_BANK_VIEW_TYPE;}getDisplayText():string{return 'Practice question bank';}getIcon():string{return 'library';}onOpen():Promise<void>{this.render();return Promise.resolve();}onClose():Promise<void>{return Promise.resolve();}render():void{const root=this.contentEl;root.empty();root.addClass('practice-question-bank-view');const header=root.createDiv({cls:'practice-view-header'});header.createEl('h2',{text:'Question bank'});const actions=header.createDiv({cls:'practice-view-actions'});actions.createEl('button',{text:'Import JSON'}).addEventListener('click',()=>void this.plugin.chooseAndPreviewImport());actions.createEl('button',{text:'Export JSON'}).addEventListener('click',()=>this.plugin.exportJson());const controls=root.createDiv({cls:'practice-filters'});const search=controls.createEl('input',{type:'search',placeholder:'Search questions...'});const subject=this.select(controls,'All subjects');const topic=this.select(controls,'All topics');const type=this.select(controls,'All types');const bank=this.plugin.repository.getBank().questions;for(const x of [...new Set(bank.map(q=>q.subject))].sort())subject.createEl('option',{value:x,text:x});for(const x of [...new Set(bank.map(q=>q.topic))].sort())topic.createEl('option',{value:x,text:x});for(const x of [...new Set(bank.map(q=>q.type))].sort())type.createEl('option',{value:x,text:x});const count=root.createEl('p',{cls:'practice-count'});const list=root.createDiv({cls:'practice-question-list'});const draw=():void=>{list.empty();const q=search.value.trim().toLocaleLowerCase();const filtered=bank.filter(x=>(!subject.value||x.subject===subject.value)&&(!topic.value||x.topic===topic.value)&&(!type.value||x.type===type.value)&&(!q||`${x.id} ${x.question} ${x.subject} ${x.topic}`.toLocaleLowerCase().includes(q)));count.setText(`${filtered.length} question(s)`);for(const x of filtered)this.card(list,x);};search.addEventListener('input',draw);subject.addEventListener('change',draw);topic.addEventListener('change',draw);type.addEventListener('change',draw);draw();}private select(parent:HTMLElement,label:string):HTMLSelectElement{const s=parent.createEl('select');s.createEl('option',{value:'',text:label});return s;}private card(parent:HTMLElement,q:Question):void{const card=parent.createDiv({cls:'practice-question-card'});card.createEl('strong',{text:q.question});card.createDiv({cls:'practice-question-meta',text:`${q.id} · ${q.subject} › ${q.topic} · ${q.type} · difficulty ${q.difficulty}`});const actions=card.createDiv({cls:'practice-card-actions'});const edit=actions.createEl('button',{title:'Edit question'});setIcon(edit,'pencil');edit.addEventListener('click',()=>new QuestionEditModal(this.app,q,async updated=>{await this.plugin.repository.updateQuestion(updated);this.render();}).open());const del=actions.createEl('button',{title:'Delete question'});setIcon(del,'trash-2');del.addEventListener('click',()=>new ConfirmDeleteModal(this.app,q.id,async()=>{await this.plugin.repository.deleteQuestion(q.id);this.render();}).open());}}
